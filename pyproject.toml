[tool.poetry]
name = "batframework"
version = "0.1.3"
description = "A pygame framework for making games easier"
authors = ["Baturay <baturayturan@gmail.com>"]
license = "LICENCE"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pygame = "^2.1.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
